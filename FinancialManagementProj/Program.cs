internal class Program
{
    public static void Main(string[] args)
    {
        //Console.WriteLine("I can't wait for Arthdal Chronicles season 2");

        /*
         * List<Transaction> transactions = new List<Transaction>();

        Transaction.AddTransaction(transactions, "Salary", 2000, new DateTime(2022, 2, 1));
        Transaction.AddTransaction(transactions, "Rent", -1000, new DateTime(2022, 2, 15));
        Transaction.AddTransaction(transactions, "Groceries", -200, new DateTime(2022, 2, 20));
        */
    }
}
